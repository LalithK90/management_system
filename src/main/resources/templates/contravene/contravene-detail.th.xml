<thlogic xmlns:th="http://www.thymeleaf.org">
    <attr sel="#header" th:replace="~{fragments/header :: headerFragment}"/>
    <attr sel="#navBar" th:replace="~{fragments/navBar :: navBarFragment}"/>

    <attr sel="#formHeader" th:text="${'Contravene Detail'}"/>
    <attr sel="#id" th:text="${contraveneDetail.id}"/>
    <attr sel="#detail" th:utext="${contraveneDetail.detail}"/>
    <attr sel="#createdBy" th:text="${contraveneDetail.createdBy}"/>
    <attr sel="#createdAt" th:text="${{contraveneDetail.createdAt}}"/>


    <attr sel="#footer" th:replace="~{fragments/footer :: footerFragment}"/>
    <attr sel="#script" th:replace="~{fragments/script :: scriptFragment}"/>
</thlogic>